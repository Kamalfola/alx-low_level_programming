#include "main.h"
/**
 * _strspn - length of a prefix substring
 * @s: input
 * @accept: input
 * Return: 0
 */
unsigned int _strspn(char *s, char *accept)
{
int n= 0;
int i;
while (*s)
{
for (i = 0; accept[i]; i++)
{
if (*s == acept[i])
{
n++;
break;
}
else if (accept [r + 1] == '\0')
return (n);
}
s++;
}
return (0);
}
