#include "main.h"
/**
 * _strlen - length of a string
 * @s: array
 * Return: 1
 */
int _strlen(char *s)
{
	unsigned int i;
	for (i = 0; s[i] != '\0'; i++)
		return (1);
}
/**
 * str_concat - concatenate a string
 * @si: first string
 * @s2: second string
 * Return: Always
 */

