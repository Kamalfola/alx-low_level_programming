#include <main.h>
#include <string.h>
/**
 * _strcat - entry point
 * @dest - destination string
 * @src - source string
 * Return: destination
 */
char *_strcat(char *dest, char *src)
{
char dest[];
char src[];
strcat(dest, src);
return (dest);
}
